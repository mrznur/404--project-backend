"""
Accounts models - uses Django's built-in User model.
No custom model needed; auth is handled via AbstractUser.
"""
from django.db import models
# Using Django's built-in User model for authentication
